---
import Layout from "../layouts/Layout.astro";
import Hero from "../components/Hero.jsx";
import TechStack from "../components/TechStack.jsx";
import ProjectCard from "../components/ProjectCard.jsx";
import Experience from "../components/Experience.jsx";
import Education from "../components/Education.jsx";
import Footer from "../components/Footer.jsx";
import { projects } from "../data/projects.js";
---

<Layout title="Fathan Yazid Satriani | Portfolio">
	<main class="relative">
		{/* Anchor for Home */}
		<div id="home" class="absolute top-0"></div>

		<Hero client:load />

		<section id="experience">
			<Experience client:only="react" />
		</section>

		<Education client:only="react" />

		<TechStack client:visible />

		<section id="projects" class="py-20 px-4 relative">
			<div class="max-w-7xl mx-auto">
				<div class="text-center mb-16">
					<h2
						class="text-center text-4xl md:text-6xl font-black uppercase tracking-tight text-white drop-shadow-[0_0_18px_rgba(255,255,255,0.35)] mb-16"
					>
						Featured Operations
					</h2>
				</div>

				<div
					class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6"
				>
					{
						projects.map((project) => (
							<ProjectCard client:load {...project} />
						))
					}
				</div>
			</div>
		</section>

		<section id="contact">
			<Footer client:visible />
		</section>
	</main>
</Layout>
